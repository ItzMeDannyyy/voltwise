// Copy this file to include/secrets.h and fill in your real values.
// include/secrets.h is gitignored — never commit real credentials.
#pragma once

// WiFi (2.4 GHz only — the ESP32 cannot join 5 GHz networks)
#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// HiveMQ Cloud (TLS MQTT)
#define MQTT_HOST     "e8438b0ef58c44b9886ce895ef33de3d.s1.eu.hivemq.cloud"
#define MQTT_PORT     8883
#define MQTT_USERNAME "your-hivemq-username"
#define MQTT_PASSWORD "your-hivemq-password"

// Identifies this device in MQTT topics (voltwise/<DEVICE_UID>/...).
// Must match MQTT_DEVICE_UID (backend) and EXPO_PUBLIC_MQTT_DEVICE_UID (app).
#define DEVICE_UID    "esp32-01"
